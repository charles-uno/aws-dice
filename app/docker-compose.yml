version: "3.9"
services:
  frontend:
    image: charlociraptor/flashcards-front-${COLOR}
    restart: always
    ports:
    - "${PORT1}:5000"
    depends_on:
    - backend
  backend:
    image: charlociraptor/flashcards-back-${COLOR}
    restart: always
    ports:
    - "${PORT2}:5001"
